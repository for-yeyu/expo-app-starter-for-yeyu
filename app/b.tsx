import { Text, View } from 'react-native';

export default function BScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>Screen B</Text>
    </View>
  );
}
